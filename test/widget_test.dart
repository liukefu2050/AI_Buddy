import 'package:ai_buddy/core/app/app.dart';
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('AIBuddy app widget can be created', () {
    expect(const AIBuddy(), isNotNull);
  });
}
